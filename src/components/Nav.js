// import React from "react";
// import { useSelector } from "react-redux";
// import { NavLink } from "react-router-dom";

// // Nav component helps us easily switch between pages for our single page app
// // useSelector hook allows us to access the Redux store's state
// const Nav = () => {
//   const students = useSelector((state) => state.students);
//   const campuses = useSelector((state) => state.campuses);

//   return (
//     <nav>
//       <NavLink to="/students" activeclassname="selected" className="nav-link">
//         Students ({students.length})
//       </NavLink>
//       <NavLink to="/campuses" activeclassname="selected" className="nav-link">
//         Campuses ({campuses.length})
//       </NavLink>
//     </nav>
//   );
// };
// // return JSX is a nav element with two NavLink elements that links to students and campuses.
// export default Nav;
